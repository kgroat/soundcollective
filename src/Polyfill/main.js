/**
 * Created by kevin on 10/6/15.
 */

(function(global){
    var local = {};
    //require('./AudioWorkerParamGlobalScope')(global, local);
    //require('./AudioWorkerParamDescriptor')(global, local);
    //require('./AudioWorkerNode')(global, local);
    //require('./AudioWorker')(global, local);
})(self || window || this);